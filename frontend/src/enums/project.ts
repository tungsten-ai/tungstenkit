enum ProjectOrder {
  LastUpdated = "last_updated",
  Stars = "stars",
  Name = "name",
}

enum StarredProjectOrder {
  RecentlyStarred = "recently_starred",
}

enum ModelOrder {
  LastUploaded = "last_uploaded",
}

export { ProjectOrder, StarredProjectOrder, ModelOrder };
